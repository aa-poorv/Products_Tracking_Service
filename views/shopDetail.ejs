<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= shop.name %> Details</title>
  </head>
  <body>
    <h1><%= shop.name %> (<%= shop.city %>)</h1>
    <ul style="list-style-type: square">
      <% for(let product of shop.products) { %>
      <li><%= product.name %> - <%= product.rate %>rs</li>
      <% } %>
    </ul>
    <a href="/shop/show">All shops</a>
  </body>
</html>
